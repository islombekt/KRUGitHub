@model IEnumerable<KRU.Models.Category111>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" type="button" class="btn btn-primary"><i class="fas fa-plus"></i> Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Category11.Category1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Category11)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name_C111)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Category11.Category1.Name_C1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category11.Name_C11)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name_C111)
            </td>

            <td>
                <a asp-action="Edit" asp-route-id="@item.Category111Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Category111Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Category111Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
